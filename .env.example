# ── SQL Server ──────────────────────────────
MSSQL_SA_PASSWORD=YourStrong@Passw0rd
MSSQL_DB=QualityDMS

# ── PostgreSQL ───────────────────────────────
POSTGRES_DB=PublicDMS
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeMePostgres!
POSTGRES_PORT=5432

# ── MongoDB ──────────────────────────────────
MONGO_USER=mongoadmin
MONGO_PASSWORD=ChangeMeMongo!

# ── FastAPI — API Key (generar con: openssl rand -hex 32) ──
FASTAPI_API_KEY=change-this-to-a-random-secret

# ── Storage compartido ───────────────────────
DMS_STORAGE_PATH=/var/dms/uploads

# ── FastAPI URL (interna entre contenedores) ──
FASTAPI_URL=http://fastapi:8000
